name: CD

on:
  push:
    branches: [ "main", "kb/docker-push" ]

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build api --tag ${{github.repository}}-api
